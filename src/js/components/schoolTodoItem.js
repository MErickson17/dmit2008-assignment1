import dataFetcher from "./../utils/dataFetcher";

/*
    toDoItem = {
        id:string
        title:string
        category:string
        isCompleted:boolean
    }
    Add start and end date/time??
*/ 

const schoolTodo = function() {
    // const data = dataFetcher('./../data/todos.json');
    // console.log(data) //its not getting the data at all...
    // const info = data[0]

    // const completed = info.completed ? "<p>Complete</p>" : "";

    // const template = `
    //     <li data-key="${info.id}">
    //         <input type="text">${info.title}</input>
    //         ${completed}
    //     </li>
    // `
    // return template
}

export default schoolTodo